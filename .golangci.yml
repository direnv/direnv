linters:
  presets:
    - bugs
    - unused
  enable:
    - gofmt
    - golint
    - misspell
    - stylecheck
  disable:
    # direnv is not a web server, context is not strictly necessary.
    - noctx
